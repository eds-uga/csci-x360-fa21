To run simply type `python3 backbone_image_classifier.py --trainer.max_epochs 2` into the command line when in PyTorch Lightning Demo 2 directory
Note: You can put any number you want after the trainer.max_epochs flag and it will run the model for that many epochs
